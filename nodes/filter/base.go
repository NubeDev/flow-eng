package filter

const (
	category = "filter"
)

// NODEs will single in/out
// only-true
// only-false
// prevent-null
// prevent-duplicates

// NODEs will double in
// only-between
// only-lower
// prevent-equal

const (
	onlyTrue    = "only-true"
	onlyFalse   = "only-false"
	preventNull = "prevent-null"
)
